# Computer networking project aims IP-Address querying using server and client systems. 
   # o Designed and implemented a project for querying IP-address for domain names
   # o Used socket programming for communication and two different systems, one as a server and other as a client is being used.
